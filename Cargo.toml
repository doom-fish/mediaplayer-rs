[package]
name = "mediaplayer"
version = "0.4.1"
edition = "2021"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/doom-fish/mediaplayer-rs"
repository = "https://github.com/doom-fish/mediaplayer-rs"
description = "Safe Rust bindings for MediaPlayer.framework on macOS — now playing, remote commands, artwork, and explicit iOS-only stubs"
authors = ["Per Johansson <per@doom.fish>"]
keywords = ["mediaplayer", "macos", "now-playing", "remote-commands", "apple"]
categories = ["api-bindings", "os::macos-apis", "multimedia"]
rust-version = "1.76"
readme = "README.md"
build = "build.rs"
include = [
    "src/**/*",
    "swift-bridge/Package.swift",
    "swift-bridge/Sources/**/*",
    "examples/**/*",
    "build.rs",
    "Cargo.toml",
    "README.md",
    "LICENSE-*",
    "CHANGELOG.md",
]

[lints.rust]

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
cargo = { level = "warn", priority = -1 }

[lib]
name = "mediaplayer"
crate-type = ["lib"]

[features]
default = []
async = ["dep:doom-fish-utils"]

[dependencies]
apple-cf = { version = ">=0.2, <0.10", features = ["cg"] }
libc = "0.2"
doom-fish-utils = { path = "../doom-fish-utils", version = ">=0.1, <0.4", optional = true }

[dev-dependencies]
pollster = "0.3"

[package.metadata.docs.rs]
all-features = true
default-target = "aarch64-apple-darwin"
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin"]
rustdoc-args = ["--cfg", "docsrs"]

[badges]
maintenance = { status = "actively-developed" }

[[example]]
name = "01_now_playing_smoke"

[[example]]
name = "14_async_streams"
required-features = ["async"]

[profile.release]
debug = "full"
strip = "none"
